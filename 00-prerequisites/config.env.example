# Copy this file to config.env and fill in the values.
# config.env is git-ignored and must never be committed.

AWS_ACCOUNT_ID=""          # Master account ID (12 digits)
AWS_REGION="us-east-1"     # Primary region

GITHUB_ORG=""              # GitHub organisation or username
GITHUB_REPO=""             # Repository name (this repo)
